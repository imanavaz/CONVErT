<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:CONVErT">

    <!--Style x:Key="BarStyle" TargetType="{x:Type local:VisualElement}" BasedOn="{StaticResource VisualElementDefaultStyle}">
        <Setter Property="Template">
            <Setter.Value>
                <DataTemplate >
                    <Border Name="bar">

                    </Border>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="VisualDecorator" TargetType="{x:Type local:VisualElement}">
        <StackPanel Orientation="Vertical" >
            <Label>Foo</Label>
            <ContentPresenter />
            <Label>Bar</Label>
        </StackPanel>
    </ControlTemplate>

    <Style x:Key="BarChartStyle" TargetType="{x:Type local:VisualElement}" BasedOn="{StaticResource VisualElementDefaultStyle}">
        <Setter Property="Template">
            <Setter.Value>
                <Border Name="Chart">

                </Border>
            </Setter.Value>
        </Setter>

    </Style>
    
    <DataTemplate DataType="{x:Type local:VisualElement}">
        <Rectangle x:Name="Bar" Width="100" Height="200"></Rectangle>
    </DataTemplate-->
    
</ResourceDictionary>